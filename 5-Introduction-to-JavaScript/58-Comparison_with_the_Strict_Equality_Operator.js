/*
  the === operator evals if two variables/values have equal value and equal type

Jean Tovar
*/
// Setup
function testStrict(val) {
  if (val === 7) { // Change this line
    return "Equal";
  }
  return "Not Equal";
}

// Change this value to test
testStrict(10);
